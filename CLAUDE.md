# SP0557 FPGA プロジェクト 解析ガイド

## プロジェクトルート
```
C:\Users\Tsuru\OneDrive\ドキュメント\GitHub\my-first-\PCBNo.10081
```

---

## 解析前に必ず確認するドキュメント

解析を開始する前に、以下の仕様書を必ず読み込むこと。

| ドキュメント | パス |
|---|---|
| SP0557設計仕様書 | `PCBNo.10081\SP0557設計仕様書.docx` |
| 回路図 | `PCBNo.10081\PCBNo.10081\PCBNO10081回路図_2508_1_09.pdf` |

仕様書から以下の情報を把握してから解析に臨むこと：
- FPGAの全体アーキテクチャとブロック構成
- 各モジュールの役割と入出力インターフェース
- 通信プロトコル仕様（S3IO等）
- レジスタマップ

---

## 解析対象ディレクトリ（RTLのみ）

### ✅ 解析対象
```
SP0557_rev02_00\SP0557_rev02_00\RTL\
├── SP0557.vhd                  ← トップレベル
├── com\                        ← 共通モジュール
│   ├── fc_dec.vhd
│   ├── fc_sel.vhd
│   └── w_pack.vhd
└── SP0557\                     ← 機能モジュール群
    ├── slv_clk_gen.vhd
    ├── ad_ctrl\                ← ADC制御
    ├── compactppmc\            ← ステッピングモータ制御
    ├── DAC43608\               ← DAC制御（I2C）
    ├── EFUSE_CTRL\             ← EFuse制御
    ├── encoder\                ← エンコーダ
    ├── gpio\                   ← GPIO
    ├── LEDLT\                  ← LED / AD8402制御
    ├── pls_sens\               ← パルスセンサ
    ├── slaveid_top\            ← スレーブID
    ├── slv_com\                ← スレーブ通信（8b10b, CRC等）
    └── sp_conv\                ← SPI変換
```

### ❌ 解析除外（ビルド成果物・IPコア生成ファイル）
```
SP0557_rev02_00\SP0557_rev02_00\SP0557\        ← ビルド出力（.bit, .ncd等）
SP0557_rev02_00\SP0557_rev02_00\RTL\IPcore\    ← IPコア生成ファイル
    dcfifo_rx_b1\, dcfifo_rx_b2\
    dcfifo_tx_b1\, dcfifo_tx_b2\
    pll_gen\
```
> IPコアの**インターフェース**確認が必要な場合は `IPcore.vhd` のみを参照すること。

---

## 解析時のルール

### モジュール解析の手順
1. まず `SP0557.vhd`（トップレベル）を読んでモジュール接続を把握する
2. 対象モジュールの `*_top.vhd` を読んで内部構成を確認する
3. サブモジュールを個別に解析する
4. 不明な信号・レジスタは仕様書のレジスタマップと照合する

### 命名規則の把握
- `slv_com` 系 → スレーブ通信関連（rx/tx, 8b10b符号化, CRC）
- `ppmc` 系 → コンパクトPPMC（ステッピングモータ制御）
- `ad_ctrl` 系 → ADC制御
- `gpio` 系 → 汎用IO

### 解析レポートのフォーマット
解析結果は以下の形式で報告すること：
```
## [モジュール名] 解析結果
- **役割**: 〇〇
- **主要入出力**: 〇〇
- **内部構成**: 〇〇
- **仕様書との対応**: 〇〇章
- **気になる点 / 要確認事項**: 〇〇
```

---

## プロジェクト概要メモ
- **デバイス**: Lattice FPGA（Synplify + Diamond使用）
- **言語**: VHDL
- **主な機能**: スレーブ通信、ステッピングモータ制御、ADC/DAC制御、GPIO、LED制御
- **通信**: S3IO（8b10b符号化、CRC付き、DCFIFO使用）